import { render } from '@testing-library/react';

import Name from './[name]';

describe('Name', () => {
    it.skip('should render successfully', async () => {
        // const { baseElement } = render(<Name name={Bulb} />);
        // expect(baseElement).toBeTruthy();
    });
});
